[project]
name = "transformer"
version = "0.1.0"
description = "Training experiments beside the Lean formalization: looped vs flat pre-norm transformers"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "sentencepiece>=0.2.2",
    "torch>=2.14.0",
]
